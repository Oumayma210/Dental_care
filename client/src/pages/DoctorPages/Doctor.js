import React from "react";
import PatientsList from "../../Components/PatientsList";

const Doctor = () => {
    return (
        <div>
            <PatientsList />
        </div>
    );
};

export default Doctor;
